#include "Examples.h"

/*
 * Starvation («голодание») — проблема в многопоточном программировании, при которой один или несколько потоков не могут получить
 * доступ к ресурсам и, следовательно, не могут выполнить свои задачи из-за постоянного монополизирования этих ресурсов другими потоками.
 * Потоки, страдающие от голодания, могут ожидать неопределённое время, в то время как другие потоки продолжают работу. Это может
 * привести к недостаточному использованию ресурсов, так как ожидающие потоки могут не выполнять полезной работы.
 *
 * Как предотвратить голодание:
 * - Использование std::this_thread::yield();
 * - Использование std::shared_mutex с таймаутами;
 * - Честная очередь (fair queue);
 * - Использование атомарных операций с backoff.
 *
 * Различия между starvation, deadlock и livelock:
 * - Deadlock: Потоки ждут друг друга циклически (взаимная блокировка)
 * - Livelock: Потоки постоянно меняют состояние, но не прогрессируют
 * - Starvation: Поток готов работать, но не может получить ресурсы
 *
 */

int main()
{
    //Example1::test();
    //Example2::test();
    Example3::test();

    return 0;
}
