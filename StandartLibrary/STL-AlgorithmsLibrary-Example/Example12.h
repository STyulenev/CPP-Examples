#pragma once

#include <algorithm>
#include <iostream>
#include <vector>

/*
 * =======================================================================================================================================================================
 *                                                                Binary search operations (on partitioned ranges)
 * =======================================================================================================================================================================
 *
 * Binary search operations:
 * - lower_bound
 * - ranges::lower_bound
 * - upper_bound
 * - ranges::upper_bound
 * - equal_range
 * - ranges::equal_range
 * - binary_search
 * - ranges::binary_search
 *
 */

namespace Example12 {

struct MyClass
{
    int id;
    std::string name;
};

namespace BSO1 { // ------------------------------------ lower_bound ()

void test()
{

}

} // namespace BSO1

} // namespace Example12
