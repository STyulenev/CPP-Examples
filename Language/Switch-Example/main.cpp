#include "Example1.h"


/*
 * switch — это оператор выбора, который позволяет выполнить один из нескольких блоков кода в зависимости от значения выражения. Он удобен,
 * когда нужно проверить значение переменной и выполнить соответствующий блок кода без использования множества if-else.
 */

int main()
{
    Example1::test();


    return 0;
}
